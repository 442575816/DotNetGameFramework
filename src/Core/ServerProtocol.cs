using System;

namespace DotNetGameFramework
{
    /// <summary>
    /// 服务协议
    /// </summary>
    public enum ServerProtocol
    {
        TCP,
        HTTP,
        UDP,
        WEBSOCKET
    }
}
